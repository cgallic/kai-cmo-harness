# Kai CMO Harness — Python Dependencies
# Install: pip install -r scripts/requirements.txt

# Core
python-dotenv>=1.0.0
pyyaml>=6.0
jinja2>=3.1.0
requests>=2.31.0

# LLM providers (install the one you use)
google-genai>=1.0.0        # For Gemini (default)
openai>=1.0.0              # OpenRouter-compatible client for agent loop + OpenAI SDK callers
# anthropic>=0.40.0        # For Claude

# Analytics
google-analytics-data>=0.18.0
google-auth>=2.20.0
google-auth-oauthlib>=1.0.0
searchconsole>=0.1.0        # Latest on PyPI is 0.1.0 — >=0.2.0 does not exist

# Data
supabase>=2.0.0
stripe>=7.0.0
aiohttp>=3.9.0             # Async lead pipeline HTTP client
feedparser>=6.0.0          # Reddit monitor and knowledge cloner RSS parsing

# Statistical analysis
scipy>=1.11.0

# Quality gates
# (no additional deps — these use stdlib + basic NLP)

# AutoReason ad loop (scripts/ads/autoreason/)
json-repair>=0.30.0

# Gateway (optional — install if running the webhook server)
# fastapi>=0.100.0
# uvicorn>=0.23.0
# pydantic>=2.0.0

# Agent loop (optional — install if running autonomous mode)
# OpenRouter client is required by agent/llm/router.py via the OpenAI SDK above.
# discord.py>=2.3.0
# twilio>=8.0.0
# apscheduler>=3.10.0
