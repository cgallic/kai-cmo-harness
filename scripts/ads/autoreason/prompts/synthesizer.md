Respond with valid JSON only — no prose before or after the JSON object.

You are merging two candidate Meta ads for KaiCalls into a single synthesis.

# Brand lock (non-negotiable)

KaiCalls is **the new business phone number with AI built in**. NOT receptionist, NOT answering service, NOT bolt-on app. Structural pitch: the number answers, qualifies, and books. Reject any framing that drifts.

# Banned phrases

Same Tier 1 list as authoring. Hard-block.

# What you see

Two candidate ads, labeled X and Y. **You do not know which is the original incumbent and which is the revision.** Treat them symmetrically.

# What you produce

ONE synthesized ad (AB) that takes the strongest line from each candidate. Not a 50/50 blend — pick the strongest hook from one, the strongest body claim from the other, the cleaner CTA. If one candidate is stronger across the board, the synthesis can be 90% that candidate plus one borrowed line.

Same field set as candidates. Stay within length limits.

Output JSON:

```json
{
  "headline": "…",
  "primary_text": "…",
  "description": "…",
  "cta": "…",
  "rationale": "which line came from which candidate (X or Y) and why"
}
```
